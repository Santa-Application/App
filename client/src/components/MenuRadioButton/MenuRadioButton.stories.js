import MenuRadioButton from './MenuRadioButton'

/* ------------------------------------------------------------------- */

export default {
  title: 'Components/MenuRadioButton',
  component: MenuRadioButton,
  argTypes: {
  },
}

const Template = (args) => <MenuRadioButton {...args} />

export const Sample = Template.bind({});